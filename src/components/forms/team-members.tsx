"use client";import {FormEvent,useEffect,useState} from "react";type Member={id:string;public_name:string;professional_title:string;role:string;privacy_mode:string};export function TeamMembers(){const [members,setMembers]=useState<Member[]>([]),[message,setMessage]=useState("");async function load(){const r=await fetch("/api/team-members");if(r.ok)setMembers(await r.json());else setMessage("Sign in with a team account.")}useEffect(()=>{void load()},[]);async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/team-members",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});setMessage(r.ok?"Team member added.":"Unable to add member.");if(r.ok){e.currentTarget.reset();await load()}}async function remove(id:string){if(!confirm("Remove this team member?"))return;await fetch(`/api/team-members?id=${id}`,{method:"DELETE"});await load()}return <div className="grid"><form className="card grid" onSubmit={add}><h2>Add a team member</h2><label>Public name or alias<input name="publicName" required/></label><label>Professional title<input name="title" required/></label><label>Team role<input name="role" required/></label><label>Biography<textarea name="bio" rows={4}/></label><label>Privacy<select name="privacyMode"><option value="name_image">Real name and image</option><option value="name_only">Real name without image</option><option value="alias">Professional alias</option><option value="anonymous">Anonymous avatar</option></select></label><button className="btn">Add member</button><p role="status">{message}</p></form><div className="card"><h2>Current team</h2>{members.length?members.map(m=><div key={m.id} style={{padding:"14px 0",borderBottom:"1px solid var(--line)",display:"flex",justifyContent:"space-between"}}><div><strong>{m.public_name}</strong><div className="muted">{m.professional_title} · {m.role} · {m.privacy_mode}</div></div><button className="btn secondary" onClick={()=>remove(m.id)}>Remove</button></div>):<div className="empty">No team members added yet.</div>}</div></div>}
