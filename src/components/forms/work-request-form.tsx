"use client";import {FormEvent,useState} from "react";export function WorkRequestForm(){const [message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMessage("Publishing…");const f=new FormData(e.currentTarget),r=await fetch("/api/work-requests",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({title:f.get("title"),description:f.get("description"),categoryId:f.get("categoryId"),skills:[],budgetMin:Number(f.get("budgetMin")),budgetMax:Number(f.get("budgetMax")),currency:f.get("currency"),visibility:f.get("visibility")})});setMessage(r.ok?"Work request published.":"Could not publish. Sign in as a client and check the fields.");if(r.ok)e.currentTarget.reset()}return <form className="card grid" onSubmit={submit}><label>Title<input name="title" required minLength={5}/></label><label>Description<textarea name="description" required minLength={30} rows={7}/></label><label>Category ID<input name="categoryId" type="text" required placeholder="Select from configured category UUID"/></label><div className="grid" style={{gridTemplateColumns:"repeat(2,1fr)"}}><label>Minimum budget (minor units)<input name="budgetMin" type="number" min="1" required/></label><label>Maximum budget (minor units)<input name="budgetMax" type="number" min="1" required/></label></div><label>Currency<select name="currency"><option>USD</option><option>EUR</option><option>TRY</option></select></label><label>Visibility<select name="visibility"><option value="public">Relevant verified talent</option><option value="invite_only">Private invitations only</option></select></label><button className="btn">Publish work request</button><p role="status">{message}</p></form>}
