"use client";
import {FormEvent,useState} from "react";
export function AIAssistant({locale="en",mode="faq"}:{locale?:string;mode?:"faq"|"talent_search"}){
  const ar=locale==="ar",[text,setText]=useState(""),[busy,setBusy]=useState(false);
  async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget),prompt=String(f.get("prompt")??"");const endpoint=mode==="talent_search"?"/api/ai/talent-search":"/api/ai";const body=mode==="talent_search"?{prompt,locale}:{task:"faq",prompt,locale};const r=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await r.json();setText(r.ok?d.text:(d.error==="service_unavailable"?(ar?"لم يتم ربط مزود الذكاء الاصطناعي بعد.":"AI provider is not connected yet."):(ar?"تعذّر تنفيذ الطلب.":"Could not answer this request.")));setBusy(false)}
  return <form className="card grid ai-card" onSubmit={submit}><div><span className="badge">GazaWorks AI</span><h2>{mode==="talent_search"?(ar?"اعثر على الموهبة الأنسب":"Find the right Gaza talent"):(ar?"مساعد المنصة":"Platform assistant")}</h2><p className="muted">{mode==="talent_search"?(ar?"صف المهارات ونوع المحترف أو الفريق الذي تحتاجه. التوصيات تعتمد فقط على بيانات GazaWorks الموثقة ولا تختلق خبرات.":"Describe the skills and type of professional or team you need. Recommendations are grounded only in verified GazaWorks database records."):(ar?"اسأل عن استخدام GazaWorks أو تحسين ملفك أو فهم إحدى ميزات المنصة.":"Ask how GazaWorks works, how to improve a profile, or how to use a platform feature.")}</p></div><label>{ar?"طلبك":"Your request"}<textarea name="prompt" required minLength={5} rows={4}/></label><button className="btn" disabled={busy}>{busy?(ar?"جارٍ العمل…":"Working…"):(ar?"اسأل GazaWorks AI":"Ask GazaWorks AI")}</button>{text&&<div className="card ai-response" style={{whiteSpace:"pre-wrap"}}>{text}</div>}</form>
}
