import {executeWorkflow} from "@/lib/workflows";
import {NextResponse} from "next/server";import {supabaseServer} from "@/lib/supabase/server";export async function GET(){const db=await supabaseServer(),{data:{user}}=await db.auth.getUser();if(!user)return NextResponse.json({error:"unauthorized"},{status:401});const {data,error}=await db.from("verification_requests").select("id,status,submitted_at,decision_reason").eq("profile_id",user.id).order("submitted_at",{ascending:false}).limit(1).maybeSingle();return error?NextResponse.json({error:"load_failed"},{status:500}):NextResponse.json(data)}
export async function POST(){return executeWorkflow("gw_request_verification",{},201);}
