import {PaymentsPanel} from "@/components/workspace/resource-panels";export const metadata={robots:{index:false}};export default function Page(){return <section className="container" style={{padding:"40px 0"}}><h1>Payments</h1><p className="muted">Financial records use integer minor units and preserve the configured total transaction deduction.</p><PaymentsPanel/></section>}
