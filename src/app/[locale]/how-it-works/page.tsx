import {CmsPage} from "@/components/cms-page";export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;return <CmsPage slug="how-it-works" locale={locale} fallbackTitle="How GazaWorks works" fallbackDescription="Discover verified talent, agree on scope, fund the project, collaborate inside GazaWorks, and complete work through a protected workflow."/>}
