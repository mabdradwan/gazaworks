import {TalentSearch} from "@/components/talent/search";import {AIAssistant} from "@/components/ai-assistant";export const metadata={robots:{index:false,follow:false}};export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;return <section className="container grid" style={{padding:"48px 0"}}><div><span className="badge">Login required · Verified talent only</span><h1 style={{fontSize:44}}>Find the right professional</h1></div><AIAssistant locale={locale} mode="talent_search"/><TalentSearch/></section>}
