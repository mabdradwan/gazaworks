"use client";import {FormEvent,useState} from "react";import {supabaseBrowser} from "@/lib/supabase/client";export default function Reset(){const [message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const email=String(new FormData(e.currentTarget).get("email"));const {error}=await supabaseBrowser().auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/en/auth`});setMessage(error?error.message:"Check your email for the secure reset link.")}return <section className="container auth-wrap"><form className="card grid" onSubmit={submit}><h1>Reset password</h1><label>Email<input name="email" type="email" required/></label><button className="btn">Send reset link</button><p role="status">{message}</p></form></section>}
